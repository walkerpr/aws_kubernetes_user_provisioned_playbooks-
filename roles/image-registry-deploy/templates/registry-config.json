{
  "auths": {
    "{{ registry_url }}": {
      "username": "{{ devops_secrets.artifactoryUser }}",
      "password": "{{ devops_secrets.artifactoryPassword }}",
      "auth": "{{ (devops_secrets.artifactoryUser + ':' + devops_secrets.artifactoryPassword) | b64encode }}"
    }
  }
}