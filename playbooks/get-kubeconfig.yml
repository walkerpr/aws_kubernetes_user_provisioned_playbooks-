---
- name: Get kubeconfig.
  hosts: cp
  become: true
  become_user: root
  tasks:
    - name: Get kubeconfig.
      include_role:
        name: kubernetes_install
        tasks_from: get_kubeconfig.yml
